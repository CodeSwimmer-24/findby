import { View, Text } from "react-native";
import HeaderHero from "./Components/Header";

const Header = () => {
  return (
    <View>
      <HeaderHero />
    </View>
  );
};

export default Header;
